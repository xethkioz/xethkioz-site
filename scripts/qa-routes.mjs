const routes = ['/', '/gaming', '/tech', '/science', '/news', '/streaming', '/media', '/community', '/green-node', '/network', '/ai-lab', '/creator-studio', '/cms', '/qa']
console.log('XETHKIOZ RC4 route checklist')
for (const route of routes) console.log(`- ${route}`)
console.log('Manual QA: open each route after npm run build + npm run preview')
