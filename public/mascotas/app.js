const places=['Puan','Darregueira','Bordenave','Villa Iris','Felipe Solá','17 de Agosto','Azopardo','Erize','San Germán'];
const basePosts=[
{id:'barby-2026',type:'Perdido',name:'Barby',species:'Perra tipo galgo',locality:'Puan',zone:'Zona céntrica',description:'Desaparecida desde el 11/07. Está castrada y tiene una C marcada en la oreja. Cualquier información puede ser importante.',phone:'2923413848',castrated:'Sí',real:true},
{id:'demo-found',type:'Encontrado',name:'Sin nombre',species:'Gato',locality:'Darregueira',zone:'Cerca de la plaza',description:'Publicación demostrativa para mostrar cómo funciona la sección.',phone:'',castrated:'Desconocido'},
{id:'demo-adopt',type:'Adopción',name:'Toby',species:'Perro',locality:'Bordenave',zone:'Hogar de tránsito',description:'Publicación demostrativa. Sociable y de tamaño mediano.',phone:'',castrated:'Sí'}
];
let stored=[];try{stored=JSON.parse(localStorage.getItem('huellas-puan-posts')||'[]')}catch{}
let posts=[...stored.filter(p=>p.id!=='barby-2026'),...basePosts];
const esc=v=>String(v??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));
function fill(select){select.innerHTML='<option value="Todas">Todas las localidades</option>'+places.map(p=>`<option>${p}</option>`).join('')}
fill(document.querySelector('#homeFilter'));document.querySelectorAll('.categoryFilter').forEach(fill);document.querySelector('#locality').innerHTML=places.map(p=>`<option>${p}</option>`).join('');
function sharePost(p){const text=`${p.type}: ${p.name} - ${p.locality}. ${p.description} ${location.origin}/mascotas/#perdidos`;navigator.share?navigator.share({title:'Huellas de Puan',text}):navigator.clipboard.writeText(text).then(()=>alert('Texto copiado para compartir.'))}
function card(p){const phone=(p.phone||'').replace(/\D/g,'');return `<article class="card"><div class="tag ${esc(p.type)}">${esc(p.type)} ${p.real?'<span class="badge real">CASO REAL</span>':''}</div><div class="card-body"><h3>${esc(p.name||'Sin nombre')}</h3><div class="meta">${esc(p.species)} · ${esc(p.locality)}</div><p>${esc(p.description)}</p><p><strong>Zona:</strong> ${esc(p.zone)}<br><strong>Castrado:</strong> ${esc(p.castrated)}</p>${phone?`<a class="contact" href="https://wa.me/54${phone}" target="_blank" rel="noopener">Contactar por WhatsApp</a>`:''}<button class="share" data-share="${esc(p.id)}">Compartir publicación</button></div></article>`}
function render(){const hf=document.querySelector('#homeFilter').value;const home=hf==='Todas'?posts:posts.filter(p=>p.locality===hf);document.querySelector('#homeGrid').innerHTML=home.length?home.map(card).join(''):'<div class="info">No hay publicaciones para esta localidad.</div>';document.querySelectorAll('.categoryGrid').forEach(grid=>{const type=grid.dataset.type,filter=grid.parentElement.querySelector('.categoryFilter').value;const list=posts.filter(p=>p.type===type&&(filter==='Todas'||p.locality===filter));grid.innerHTML=list.length?list.map(card).join(''):'<div class="info">Todavía no hay publicaciones.</div>'});document.querySelectorAll('[data-share]').forEach(b=>b.onclick=()=>sharePost(posts.find(p=>p.id===b.dataset.share)))}
function show(id){document.querySelectorAll('.section').forEach(s=>s.classList.toggle('active',s.id===id));document.querySelectorAll('.nav button').forEach(b=>b.classList.toggle('active',b.dataset.section===id));history.replaceState(null,'','#'+id);scrollTo({top:0,behavior:'smooth'})}
document.querySelectorAll('[data-section]').forEach(b=>b.onclick=()=>show(b.dataset.section));document.querySelectorAll('[data-go]').forEach(b=>b.onclick=()=>{if(b.dataset.type)document.querySelector('#type').value=b.dataset.type;show(b.dataset.go)});document.querySelectorAll('#homeFilter,.categoryFilter').forEach(s=>s.onchange=render);
document.querySelector('#postForm').onsubmit=e=>{e.preventDefault();const f=new FormData(e.currentTarget);const p={id:crypto.randomUUID(),type:f.get('type'),name:f.get('name')||'Sin nombre',species:f.get('species'),locality:f.get('locality'),zone:f.get('zone'),description:f.get('description'),phone:f.get('phone'),castrated:f.get('castrated')};stored=[p,...stored];localStorage.setItem('huellas-puan-posts',JSON.stringify(stored));posts=[...stored.filter(x=>x.id!=='barby-2026'),...basePosts];e.currentTarget.reset();const n=document.querySelector('#notice');n.style.display='block';n.textContent='La publicación quedó guardada en este dispositivo. La conexión comunitaria con Supabase será la próxima etapa.';render()};
render();const initial=location.hash.slice(1);if(document.getElementById(initial))show(initial);