const places=['Puan','Darregueira','Bordenave','Villa Iris','Felipe Solá','17 de Agosto','Azopardo','Erize','San Germán'];
const basePosts=[
{id:'barby-2026',type:'Perdido',name:'Barby',species:'Perra tipo galgo',locality:'Puan',zone:'Zona céntrica',description:'Desaparecida desde el 11/07. Está castrada y tiene una C marcada en la oreja. Cualquier información puede ser importante.',phone:'2923413848',castrated:'Sí',real:true,imageCount:0},
{id:'demo-found',type:'Encontrado',name:'Sin nombre',species:'Gato',locality:'Darregueira',zone:'Cerca de la plaza',description:'Publicación demostrativa para mostrar cómo funciona la sección.',phone:'',castrated:'Desconocido',imageCount:0},
{id:'demo-adopt',type:'Adopción',name:'Toby',species:'Perro',locality:'Bordenave',zone:'Hogar de tránsito',description:'Publicación demostrativa. Sociable y de tamaño mediano.',phone:'',castrated:'Sí',imageCount:0}
];
let stored=[];try{stored=JSON.parse(localStorage.getItem('huellas-puan-posts')||'[]')}catch{}
let posts=[...stored.filter(p=>!basePosts.some(b=>b.id===p.id)),...basePosts];
let selectedImages=[];
const MAX_IMAGES=2;
const esc=v=>String(v??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));
function fill(select){select.innerHTML='<option value="Todas">Todas las localidades</option>'+places.map(p=>`<option>${p}</option>`).join('')}
fill(document.querySelector('#homeFilter'));document.querySelectorAll('.categoryFilter').forEach(fill);document.querySelector('#locality').innerHTML=places.map(p=>`<option>${p}</option>`).join('');

const publishLead=document.querySelector('#publicar .lead');
if(publishLead)publishLead.textContent='Podés cargar hasta 2 imágenes. No publiques domicilios exactos: indicá una zona aproximada.';
const imageHelp=document.querySelector('.image-help');
if(imageHelp)imageHelp.textContent='Máximo 2 archivos. Formatos JPG, PNG o WebP. Las imágenes se guardan localmente hasta conectar Supabase.';

const DB_NAME='huellas-puan-media',STORE='images';
function openDb(){return new Promise((resolve,reject)=>{const req=indexedDB.open(DB_NAME,1);req.onupgradeneeded=()=>{const db=req.result;if(!db.objectStoreNames.contains(STORE))db.createObjectStore(STORE,{keyPath:'key'})};req.onsuccess=()=>resolve(req.result);req.onerror=()=>reject(req.error)})}
async function saveImages(postId,files){if(!files.length)return;const db=await openDb();await new Promise((resolve,reject)=>{const tx=db.transaction(STORE,'readwrite'),store=tx.objectStore(STORE);files.forEach((file,index)=>store.put({key:`${postId}:${index}`,postId,index,blob:file}));tx.oncomplete=resolve;tx.onerror=()=>reject(tx.error)})}
async function getImages(postId,count){if(!count)return[];const db=await openDb();return Promise.all(Array.from({length:Math.min(count,MAX_IMAGES)},(_,index)=>new Promise(resolve=>{const tx=db.transaction(STORE,'readonly');const req=tx.objectStore(STORE).get(`${postId}:${index}`);req.onsuccess=()=>resolve(req.result?.blob||null);req.onerror=()=>resolve(null)}))).then(items=>items.filter(Boolean))}

function sharePost(p){if(!p)return;const section=p.type==='Perdido'?'perdidos':p.type==='Encontrado'?'encontrados':'adopciones';const text=`${p.type}: ${p.name} - ${p.locality}. ${p.description} ${location.origin}/mascotas/#${section}`;if(navigator.share){navigator.share({title:'Huellas de Puan',text}).catch(()=>{})}else if(navigator.clipboard){navigator.clipboard.writeText(text).then(()=>alert('Texto copiado para compartir.'))}}
function card(p){const phone=(p.phone||'').replace(/\D/g,'');const gallery=p.imageCount?`<div class="gallery" data-gallery="${esc(p.id)}" data-count="${Math.min(Number(p.imageCount)||0,MAX_IMAGES)}" aria-label="Imágenes de ${esc(p.name)}"></div>`:'';return `<article class="card">${gallery}<div class="tag ${esc(p.type)}">${esc(p.type)} ${p.real?'<span class="badge real">CASO REAL</span>':''}</div><div class="card-body"><h3>${esc(p.name||'Sin nombre')}</h3><div class="meta">${esc(p.species)} · ${esc(p.locality)}</div><p>${esc(p.description)}</p><p><strong>Zona:</strong> ${esc(p.zone)}<br><strong>Castrado:</strong> ${esc(p.castrated)}</p>${phone?`<a class="contact" href="https://wa.me/54${phone}" target="_blank" rel="noopener">Contactar por WhatsApp</a>`:''}<button class="share" data-share="${esc(p.id)}">Compartir publicación</button></div></article>`}
async function hydrateGalleries(){document.querySelectorAll('[data-gallery]').forEach(async gallery=>{const blobs=await getImages(gallery.dataset.gallery,Number(gallery.dataset.count));gallery.innerHTML='';blobs.forEach((blob,index)=>{const img=document.createElement('img');img.src=URL.createObjectURL(blob);img.alt=`Imagen ${index+1} de la publicación`;img.onload=()=>URL.revokeObjectURL(img.src);gallery.appendChild(img)})})}
function render(){const hf=document.querySelector('#homeFilter').value;const home=hf==='Todas'?posts:posts.filter(p=>p.locality===hf);document.querySelector('#homeGrid').innerHTML=home.length?home.map(card).join(''):'<div class="info">No hay publicaciones para esta localidad.</div>';document.querySelectorAll('.categoryGrid').forEach(grid=>{const type=grid.dataset.type,filter=grid.parentElement.querySelector('.categoryFilter').value;const list=posts.filter(p=>p.type===type&&(filter==='Todas'||p.locality===filter));grid.innerHTML=list.length?list.map(card).join(''):'<div class="info">Todavía no hay publicaciones.</div>'});document.querySelectorAll('[data-share]').forEach(b=>b.onclick=()=>sharePost(posts.find(p=>p.id===b.dataset.share)));hydrateGalleries()}
function show(id){document.querySelectorAll('.section').forEach(s=>s.classList.toggle('active',s.id===id));document.querySelectorAll('.nav button').forEach(b=>b.classList.toggle('active',b.dataset.section===id));history.replaceState(null,'','#'+id);scrollTo({top:0,behavior:'smooth'})}
document.querySelectorAll('[data-section]').forEach(b=>b.onclick=()=>show(b.dataset.section));document.querySelectorAll('[data-go]').forEach(b=>b.onclick=()=>{if(b.dataset.type)document.querySelector('#type').value=b.dataset.type;show(b.dataset.go)});document.querySelectorAll('#homeFilter,.categoryFilter').forEach(s=>s.onchange=render);

const imageInput=document.querySelector('#images'),preview=document.querySelector('#imagePreview');
function renderPreview(){preview.innerHTML='';selectedImages.forEach((file,index)=>{const img=document.createElement('img');img.src=URL.createObjectURL(file);img.alt=`Vista previa ${index+1}`;img.onload=()=>URL.revokeObjectURL(img.src);preview.appendChild(img)})}
imageInput.onchange=()=>{const files=[...imageInput.files];if(files.length>MAX_IMAGES){alert(`Podés cargar hasta ${MAX_IMAGES} imágenes por publicación.`);imageInput.value='';selectedImages=[];renderPreview();return}const invalid=files.find(file=>!['image/jpeg','image/png','image/webp'].includes(file.type));if(invalid){alert('Usá imágenes JPG, PNG o WebP.');imageInput.value='';selectedImages=[];renderPreview();return}selectedImages=files;renderPreview()};

document.querySelector('#postForm').onsubmit=async e=>{e.preventDefault();const button=e.currentTarget.querySelector('.submit');button.disabled=true;button.textContent='Guardando…';try{const f=new FormData(e.currentTarget),id=crypto.randomUUID();const p={id,type:f.get('type'),name:f.get('name')||'Sin nombre',species:f.get('species'),locality:f.get('locality'),zone:f.get('zone'),description:f.get('description'),phone:f.get('phone'),castrated:f.get('castrated'),imageCount:selectedImages.length};await saveImages(id,selectedImages);stored=[p,...stored];localStorage.setItem('huellas-puan-posts',JSON.stringify(stored));posts=[...stored.filter(x=>!basePosts.some(b=>b.id===x.id)),...basePosts];e.currentTarget.reset();selectedImages=[];renderPreview();const n=document.querySelector('#notice');n.style.display='block';n.textContent='Publicación e imágenes guardadas en este dispositivo. La próxima etapa conectará todo con Supabase para compartirlo con la comunidad.';render()}catch{alert('No se pudo guardar la publicación. Revisá el espacio disponible del navegador.')}finally{button.disabled=false;button.textContent='Publicar información'}};
render();const initial=location.hash.slice(1);if(document.getElementById(initial))show(initial);