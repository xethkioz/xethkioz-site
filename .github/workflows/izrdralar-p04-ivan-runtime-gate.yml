name: Izrdralar P04 Ivan Runtime Gate

on:
  workflow_dispatch:
  pull_request:
    paths:
      - 'src/npc/ivan_runtime_visual.gd'
      - 'src/npc/ivan_scientist_runtime.gd'
      - 'src/world/izrdralar_m01_m05_runtime_visual_pass13.gd'
      - 'scenes/izrdralar/IzrdralarM01M05Runtime.tscn'
      - 'tools/izrdralar_p04_ivan_runtime_smoke.gd'
      - 'tools/scenes/IzrdralarP04IvanRuntimeSmoke.tscn'
      - '.github/workflows/izrdralar-p04-ivan-runtime-gate.yml'

concurrency:
  group: izrdralar-p04-ivan-${{ github.event.pull_request.number || github.ref_name }}
  cancel-in-progress: true

jobs:
  p04-ivan:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4

      - name: Install Godot 4.7.2
        shell: bash
        run: |
          set -euxo pipefail
          wget -q https://godot-releases.nbg1.your-objectstorage.com/4.7.2-stable/Godot_v4.7.2-stable_linux.x86_64.zip -O /tmp/godot.zip
          unzip -q /tmp/godot.zip -d /tmp/godot
          chmod +x /tmp/godot/Godot_v4.7.2-stable_linux.x86_64
          /tmp/godot/Godot_v4.7.2-stable_linux.x86_64 --version

      - name: Import project and reject parser regressions
        shell: bash
        run: |
          set -o pipefail
          GODOT=/tmp/godot/Godot_v4.7.2-stable_linux.x86_64
          "$GODOT" --headless --path . --editor --quit 2>&1 | tee /tmp/p04-import.log
          if grep -E "SCRIPT ERROR|Parse Error" /tmp/p04-import.log; then
            exit 1
          fi

      - name: Validate P04 Ivan canon and runtime contract
        shell: bash
        run: |
          set -euxo pipefail
          GODOT=/tmp/godot/Godot_v4.7.2-stable_linux.x86_64
          "$GODOT" --headless --path . res://tools/scenes/IzrdralarP04IvanRuntimeSmoke.tscn 2>&1 | tee /tmp/p04-smoke.log
          grep -q "IZRDRALAR_P04_IVAN_RUNTIME_PASS" /tmp/p04-smoke.log

      - name: Upload P04 logs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: izrdralar-p04-ivan-runtime-gate-logs
          path: |
            /tmp/p04-import.log
            /tmp/p04-smoke.log
          if-no-files-found: warn
