name: Izrdralar Progression Gate

on:
  workflow_dispatch:
  pull_request:
    paths:
      - 'src/core/game_state.gd'
      - 'src/player/player_controller*.gd'
      - 'tools/izrdralar_level_progression_smoke.gd'
      - 'tools/scenes/IzrdralarLevelProgressionSmoke.tscn'
      - '.github/workflows/izrdralar-progression-gate.yml'

concurrency:
  group: izrdralar-progression-${{ github.event.pull_request.number || github.ref_name }}
  cancel-in-progress: true

jobs:
  progression:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4

      - name: Install Godot 4.7.2
        shell: bash
        run: |
          set -euxo pipefail
          wget -q https://godot-releases.nbg1.your-objectstorage.com/4.7.2-stable/Godot_v4.7.2-stable_linux.x86_64.zip -O /tmp/godot.zip
          unzip -q /tmp/godot.zip -d /tmp/godot
          chmod +x /tmp/godot/Godot_v4.7.2-stable_linux.x86_64
          /tmp/godot/Godot_v4.7.2-stable_linux.x86_64 --version

      - name: Import project and reject parser regressions
        shell: bash
        run: |
          set -o pipefail
          GODOT=/tmp/godot/Godot_v4.7.2-stable_linux.x86_64
          "$GODOT" --headless --path . --editor --quit 2>&1 | tee /tmp/progression-import.log
          if grep -E "SCRIPT ERROR|Parse Error" /tmp/progression-import.log; then
            exit 1
          fi

      - name: Level progression smoke
        shell: bash
        run: |
          set -euxo pipefail
          GODOT=/tmp/godot/Godot_v4.7.2-stable_linux.x86_64
          "$GODOT" --headless --path . res://tools/scenes/IzrdralarLevelProgressionSmoke.tscn 2>&1 | tee /tmp/progression-smoke.log
          grep -q "IZRDRALAR_LEVEL_PROGRESSION_PASS" /tmp/progression-smoke.log

      - name: Upload progression logs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: izrdralar-progression-gate-logs
          path: |
            /tmp/progression-import.log
            /tmp/progression-smoke.log
          if-no-files-found: warn
